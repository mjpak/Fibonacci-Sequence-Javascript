function fibonacciGenerator (n) {

var fib = [];
    for(var i=0; i < n; i++)
   {
      if(i === 0)
        fib.push(0);
      else if(i === 1)
        fib.push(1);
      else
        fib.push(fib[fib.length-1]+fib[fib.length-2]);
    }
    return fib;  
 

output = fibunacciGenerator();
console.log(fib)
